services:
  nextflow:
    image: nextflow/nextflow:25.10.4
    volumes:
      - .:/workspace
      - ./gcp-creds:/root/.config/gcloud:ro
    working_dir: /workspace/nextflow-gatk
    entrypoint: [/bin/bash]
    